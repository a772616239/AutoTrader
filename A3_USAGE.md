# AutoTrader - A3 策略新增说明

## 📋 快速开始

### 运行 A3 策略
```bash
python main.py --strategy a3
```

### 运行其他策略
```bash
python main.py --strategy a1  # A1：动量反转策略
python main.py --strategy a2  # A2：Z-Score均值回归策略
python main.py --strategy a3  # A3：双均线成交量突破策略（新）
```

## 🎯 A3 策略详解

### 核心逻辑
A3 策略（双均线 + 成交量突破）是一个**趋势追踪 + 动量确认**的策略。

#### 买入信号（Golden Cross）
```
条件1：快速均线 (EMA 9) 向上穿过慢速均线 (EMA 21) ✅ 金叉
条件2：价格在慢速均线上方 ✅ 
条件3：成交量 > 20周期成交量均线 × 1.5 倍 ✅ 成交量突破
条件4：当前成交量 > 500,000 ✅ 最小成交量

→ 所有条件满足 → 生成买入信号
```

#### 卖出信号（Death Cross）
```
条件1：快速均线 (EMA 9) 向下穿过慢速均线 (EMA 21) ✅ 死叉

→ 条件满足 → 生成卖出信号
```

### 参数说明

| 参数 | 默认值 | 含义 |
|------|-------|------|
| **fast_ma_period** | 9 | 快速 EMA 周期（日间数据为分钟） |
| **slow_ma_period** | 21 | 慢速 EMA 周期 |
| **ema_or_sma** | 'EMA' | 使用指数还是简单均线 |
| **volume_sma_period** | 20 | 成交量均线周期 |
| **volume_surge_ratio** | 1.5 | 成交量突破倍数（1.5 = 需要突破平均 50%） |
| **min_volume_threshold** | 500,000 | 最小成交量要求 |
| **take_profit_pct** | 0.03 | 止盈目标（3%） |
| **max_holding_minutes** | 60 | 最大持仓时间（60 分钟） |
| **avoid_open_hour** | True | 避开开盘第一小时 |
| **avoid_close_hour** | True | 避开收盘最后一小时 |

### 风险管理

1. **单笔交易上限**：$10,000（per_trade_notional_cap）
2. **单股仓位上限**：$60,000（max_position_notional）
3. **止盈百分比**：3%
4. **持仓时间限制**：60 分钟内必须平仓
5. **活跃持仓限制**：最多 5 个同时持仓

## 📊 策略对比

```
                    A1 (动量反转)        A2 (Z-Score)         A3 (双均线)
────────────────────────────────────────────────────────────────────
风险等级            中高                低                    中
交易频率            高                  中                    中-低
胜率预期            45-55%              50-60%               55-65%
年化收益            15-25%              10-20%               20-30%
最大回撤            8-12%               6-10%                5-10%
适用市场            高波动              震荡范围             明确趋势
学习难度            中等                简单                 中等
```

## 🔧 参数调优建议

### 保守交易（低频率、高胜率）
```python
{
    'fast_ma_period': 12,        # 反应稍微慢一点
    'slow_ma_period': 26,        # 更长周期确认趋势
    'volume_surge_ratio': 2.0,   # 更高的成交量要求
    'take_profit_pct': 0.04,     # 更高的止盈目标
    'max_holding_minutes': 30,   # 更短的持仓时间
}
```

### 激进交易（高频率、追求最大收益）
```python
{
    'fast_ma_period': 6,         # 快速反应
    'slow_ma_period': 15,        # 快速确认
    'volume_surge_ratio': 1.3,   # 更低的成交量要求
    'take_profit_pct': 0.02,     # 较低的止盈目标
    'max_holding_minutes': 90,   # 更长的持仓时间
}
```

### 均衡交易（推荐配置）
```python
{
    'fast_ma_period': 9,         # 标准配置
    'slow_ma_period': 21,
    'volume_surge_ratio': 1.5,
    'take_profit_pct': 0.03,
    'max_holding_minutes': 60,
}
```

## 📈 示例交易流程

```
时间       股票    信号类型        成交价  数量    仓位价值  备注
────────────────────────────────────────────────────────────
09:45     AAPL    SCAN START
10:15     AAPL    📊 快速EMA穿过慢速EMA (150 > 149.8)
          AAPL    ✅ 成交量: 1.2M > 平均 800K × 1.5
          AAPL    🟢 BUY SIGNAL   $150.00  66    $9,900    (置信度 0.85)
10:16     AAPL    ✅ 执行买入订单
          AAPL    持仓: 66股 @ $150.00
11:15     AAPL    📊 快速EMA还在慢速EMA上方
          AAPL    持仓: 66股 @ $150.80 (+0.5%)
11:30     AAPL    ⚠️  快速EMA穿过慢速EMA (下穿)
          AAPL    🔴 SELL SIGNAL  $151.50   -     +$990    (+1%)
11:31     AAPL    ✅ 执行卖出订单
          AAPL    交易完成 盈利: $990 (+1%)
```

## 🚨 常见问题

### Q1: A3 策略和 A1/A2 的主要区别是什么？
**A**: 
- **A1** 是动量反转，当价格过度偏离时反向交易
- **A2** 是均值回归，统计学方法识别超卖机会
- **A3** 是趋势追踪，识别新的上升趋势并追踪

### Q2: 什么时候应该用 A3 而不是 A1/A2？
**A**: 
- 当股票处于明确的上升趋势时 → 用 A3
- 当股票在范围内波动时 → 用 A2
- 当股票频繁反转时 → 用 A1

### Q3: 为什么我的 A3 没有交易信号？
**A**: 
1. 检查股票是否在交易时间内（09:45-15:30，避开开收盘）
2. 检查是否有足够的历史数据（至少 30 根 K 线）
3. 检查成交量是否足够（> 500,000）
4. 检查金叉是否已经形成（需要两根 K 线的快速均线关系变化）

### Q4: 应该如何选择参数？
**A**: 
1. 从默认参数开始
2. 使用回测数据进行参数优化
3. 在纸面交易中验证
4. 逐步应用到实盘

### Q5: A3 的止盈为什么这么低（只有 3%）？
**A**: 
- 日内交易风险高，需要快速获利了结
- 避免持仓过长被反向成交
- 3% 目标易达成，可积少成多

## 💡 优化建议

### 1. 多周期确认
```python
# 在不同时间周期上都检查金叉
fast_ma_5m = calculate_ma(data_5m, 9)   # 5分钟线
fast_ma_15m = calculate_ma(data_15m, 9) # 15分钟线
# 两条都发出买入信号时才交易
```

### 2. 结合其他指标
```python
# 添加 RSI 过滤
if rsi > 50:  # 动量向上
    if golden_cross:  # 且出现金叉
        place_buy_signal()
```

### 3. 动态止盈
```python
# 根据 ATR 动态设置止盈
atr = calculate_atr(data)
take_profit = entry_price + atr * 2.0
```

## 📚 关键文件

- **`strategies/a3_dual_ma_volume.py`** - A3 策略实现
- **`A3_STRATEGY_GUIDE.md`** - 详细策略指南
- **`config.py`** - 全局配置
- **`main.py`** - 主程序入口

## 🎓 下一步

建议后续实现的策略：
- **A4**: Bollinger Bands + RSI（范围突破策略）
- **A5**: MACD + Volume Break（趋势反转策略）
- **A6**: 多策略融合（智能策略选择）

---

**最后更新**: 2025-12-06  
**版本**: 1.0 - A3 策略初始版本

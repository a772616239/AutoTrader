# AutoTrader 三大策略快速参考卡

## 🎯 策略选择决策树

```
                    你的市场观点
                         │
                ┌────────┼────────┐
                │        │        │
            高波动    范围震荡    明确趋势
                │        │        │
                ↓        ↓        ↓
              A1       A2        A3
            (动量反转) (Z-Score) (双均线)
```

## 📊 三大策略速查表

### A1 - 动量反转策略 (Momentum Reversal)
```
何时用：股票剧烈波动时
买入：RSI > 72 且价格 > MA + 2.5σ 且成交量↑
卖出：RSI < 28 或持仓 > 120分钟
命令：python main.py --strategy a1
文档：strategies/a1_momentum_reversal.py
```

### A2 - Z-Score均值回归 (Mean Reversion)
```
何时用：股票在范围内来回摆动时
买入：Z-Score < -2.0 且成交量↑
卖出：Z-Score > -0.5 或持仓 > 5天
命令：python main.py --strategy a2
文档：strategies/a2_zscore.py
```

### A3 - 双均线成交量突破 (Dual MA + Volume) ✨
```
何时用：股票处于明确上升趋势时
买入：快速EMA(9) ↑穿 慢速EMA(21) 且成交量 > 平均×1.5
卖出：快速EMA(9) ↓穿 慢速EMA(21) 或持仓 > 60分钟
命令：python main.py --strategy a3
文档：strategies/a3_dual_ma_volume.py
```

## ⚙️ 通用配置参数

所有策略通用风险管理参数：
```
单笔交易上限           $10,000
单股总仓位上限         $60,000
最大同时持仓数         5 个
最小现金缓冲           30%
```

## 🚀 快速启动命令

```bash
# 启动 A1 动量反转策略
python main.py --strategy a1

# 启动 A2 Z-Score策略
python main.py --strategy a2

# 启动 A3 双均线成交量策略（新）
python main.py --strategy a3

# 交互模式
python main.py --strategy a3 --interactive

# 查看帮助
python main.py --help
```

## 📋 A3 参数速查

| 参数 | 默认值 | 调大 | 调小 |
|------|-------|------|------|
| fast_ma_period | 9 | 反应变慢 | 反应变快 |
| slow_ma_period | 21 | 确认变慢 | 确认变快 |
| volume_surge_ratio | 1.5 | 信号减少 | 信号增加 |
| take_profit_pct | 3% | 收益减少 | 收益增加但风险↑ |
| max_holding_minutes | 60 | 持仓时间长 | 持仓时间短 |

## 🔍 问题诊断

### 没有交易信号？
- [ ] 检查股票是否在交易时间 (09:45-15:30)
- [ ] 检查成交量是否 > 500,000
- [ ] 检查历史数据是否足够 (> 30 根 K 线)
- [ ] 检查是否避开了开收盘 (09:30-10:30, 14:30-16:00)

### 交易频率太高？
- 调高 `volume_surge_ratio` (1.5 → 2.0)
- 调高 `fast_ma_period` (9 → 12)
- 调高 `slow_ma_period` (21 → 26)

### 交易频率太低？
- 调低 `volume_surge_ratio` (1.5 → 1.3)
- 调低 `fast_ma_period` (9 → 6)
- 调低 `slow_ma_period` (21 → 15)

### 盈利太少？
- 调低 `take_profit_pct` (3% → 2%)
- 调长 `max_holding_minutes` (60 → 90)
- 使用激进配置 (见下表)

### 亏损太大？
- 调高 `take_profit_pct` (3% → 4%)
- 调短 `max_holding_minutes` (60 → 30)
- 使用保守配置 (见下表)

## ⚡ 三种配置预设

### 保守配置（低风险、低收益）
```python
{
    'fast_ma_period': 12,
    'slow_ma_period': 26,
    'volume_surge_ratio': 2.0,
    'take_profit_pct': 0.04,
    'max_holding_minutes': 30,
    'min_volume_threshold': 1000000,
}
```

### 均衡配置（推荐）
```python
{
    'fast_ma_period': 9,
    'slow_ma_period': 21,
    'volume_surge_ratio': 1.5,
    'take_profit_pct': 0.03,
    'max_holding_minutes': 60,
    'min_volume_threshold': 500000,
}
```

### 激进配置（高风险、高收益）
```python
{
    'fast_ma_period': 6,
    'slow_ma_period': 15,
    'volume_surge_ratio': 1.3,
    'take_profit_pct': 0.02,
    'max_holding_minutes': 90,
    'min_volume_threshold': 300000,
}
```

## 💡 最佳实践

1. **从默认参数开始** - 先用推荐配置
2. **在历史数据上回测** - 验证参数有效性
3. **纸面交易验证** - 模拟真实环境测试
4. **小额实盘试水** - 逐步增加仓位
5. **持续监控调优** - 根据市场状态调整

## 📈 性能预期

| 指标 | A1 | A2 | A3 |
|------|----|----|-----|
| 年化收益率 | 15-25% | 10-20% | 20-30% |
| 最大回撤 | 8-12% | 6-10% | 5-10% |
| 夏普比率 | 1.5-2.0 | 1.2-1.8 | 1.8-2.5 |
| 胜率 | 45-55% | 50-60% | 55-65% |
| 平均交易时长 | 20-40分钟 | 数小时-天 | 30-90分钟 |

> 实际结果取决于参数、市场环境和风险管理

## 🔗 相关文档

- **详细指南**: `A3_STRATEGY_GUIDE.md`
- **使用手册**: `A3_USAGE.md`
- **实现报告**: `A3_IMPLEMENTATION_REPORT.md`
- **A1 策略**: `strategies/a1_momentum_reversal.py`
- **A2 策略**: `strategies/a2_zscore.py`
- **A3 策略**: `strategies/a3_dual_ma_volume.py` ✨

---

**最后更新**: 2025-12-06  
**下次更新**: 当添加 A4/A5 策略时
